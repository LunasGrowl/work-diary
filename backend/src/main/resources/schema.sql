CREATE TABLE IF NOT EXISTS entry (
  id INT AUTO_INCREMENT PRIMARY KEY,
  entry_day varchar(255),
  entry_date varchar(255),
  entry_content varchar(1023)
  );

 -- Insert into entry Values (1,'dsa','dsadsad','321321');